#include <iostream>
#include <bitset>
using namespace std;
int main(){
    int t;
    cin>>t;
    while (t--){
        long long n;
        cin>>n;
        if (__builtin_popcountll(n)>1){
            cout<<"yes"<<endl;
        }
        else {
            cout<<"No"<<endl;
        }

        //T.C.:- O(1);
        //S.C.:- O(1);
    }
}
